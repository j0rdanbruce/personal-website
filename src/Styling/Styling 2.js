/**
 * A file to store styling variables to avoid hard coding repeating styling themes throughout the applicaiton.
 */

const darkNavyBlueColorTheme = rgb(12,15,26);
const lightNavyBlueColorTheme = rgb(12,15,26);
const brightBlueColor = rgb(0,122,255);